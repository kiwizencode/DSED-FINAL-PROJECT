@model IEnumerable<DSED_FINAL.Models.Supplier>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Address01)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Address02)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Address03)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Phone)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Fax)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Address01)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Address02)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Address03)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Phone)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Fax)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdPk">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdPk">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdPk">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
